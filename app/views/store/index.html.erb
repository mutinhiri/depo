<% if notice %>
  <aside id="notice"><%= notice %>   
<%end%>

<h1> Your Catalog page </h1>
<ul class="catalog">
  <% @products.each do |product|%>
    <li>
      <%= image_tag(product.image_url)%>
      <h2><%= product.title%></h2>
      <p>
        <%= sanitize(product.description) %>

</ul>